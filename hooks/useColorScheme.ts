/**
 * Hardcoding the app to light mode for now.
 *
 * Eventually, we'll add in a dark mode: https://necolas.github.io/react-native-web/docs/use-color-scheme/
 */
export function useColorScheme(): "light" {
  return "light";
}
